# Frontend RC3 Rollback

Restore the prior immutable frontend directory through the atomic symlink, verify Bank Transfer and
authentication routes, and retain the matching backend/configuration contract. Do not edit deployed
files or remove historical data. Coordinate frontend rollback with backend mode configuration.
